# Copy to packages/db/.env (Prisma reads .env from the package it runs in) and to
# any service that talks to Postgres. Matches the docker-compose.yml credentials.
DATABASE_URL="postgresql://imaia:imaia@localhost:5433/imaia?schema=public"
